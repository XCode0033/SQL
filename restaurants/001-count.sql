
use restaurants;
-- SELECT COUNT(*) FROM bookings; -- looked at all individual entries

-- SELECT COUNT(booking_date) FROM bookings; -- 20 entries ^ means 20 bookings

SELECT COUNT(amount_tipped) FROM booking;